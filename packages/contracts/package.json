{
  "name": "@app/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Schemas Zod 4 compartidos entre frontend y backend · Plazapp",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "echo \"(lint delegado a raíz)\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
