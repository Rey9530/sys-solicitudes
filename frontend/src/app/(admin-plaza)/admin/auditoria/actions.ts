'use server';

/**
 * T-161: archivo de acciones de la página `/admin/auditoria`.
 *
 * Esta página es READ-ONLY — el visor no muta nada, solo consulta. Si en el
 * futuro se agrega una acción de admin (p.ej. "marcar revisado"), se exporta
 * acá con el patrón estándar `ActionResult` de las otras páginas del repo.
 */
export {};
